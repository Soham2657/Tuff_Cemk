services:
  - type: web
    name: tuffcemk-frontend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: VITE_BACKEND_URL
        value: https://backend-tuffcemk.onrender.com
